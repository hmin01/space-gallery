import "./globals.css";
import { Inter } from "next/font/google";
import ChakraConfigProvder from "@/components/providers/ChakraConfigProvider";
// React Query
// import ReactQueryProvider from "@/components/providers/ReactQueryProvider";

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "Space Gallery",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
  modal,
}: {
  children: React.ReactNode;
  modal: React.ReactNode;
}) {
  return (
    // <ReactQueryProvider>
    <html lang="en">
      <body className={`bg-black min-h-screen p-4 ${inter.className}`}>
        <ChakraConfigProvder>
          <>{children}</>
          <>{modal}</>
        </ChakraConfigProvder>
      </body>
    </html>
    // </ReactQueryProvider>
  );
}
